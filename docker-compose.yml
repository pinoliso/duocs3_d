version: '1'
services:
  microservicio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - spring.datasource.url=jdbc:oracle:thin:@t8cbne3y507zuv36_high?TNS_ADMIN=/app/oracle_wallet/
      - spring.datasource.username=fullstack
      - spring.datasource.password=Atangana.123
    restart: always